interface:
  display_name: "Harness Artifact"
  short_description: "Create durable HTML project views"
  default_prompt: "Use $harness-artifact to turn this project explanation into a durable, self-contained HTML view."
policy:
  allow_implicit_invocation: true
