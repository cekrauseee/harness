interface:
  display_name: "Harness Worktree"
  short_description: "Manage host-stored worktree lifecycles"
  default_prompt: "Use $harness-worktree to create and manage an isolated worktree while letting the active host choose its storage path."
