interface:
  display_name: "Harness Pull Request"
  short_description: "Prepare evidence-based pull requests"
  default_prompt: "Use $harness-pr to derive a truthful pull request from the actual diff and available verification."
policy:
  allow_implicit_invocation: true
