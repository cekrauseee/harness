interface:
  display_name: "Harness Review"
  short_description: "Review changes with grounded severity"
  default_prompt: "Use $harness-review to inspect these changes read-only and report only evidence-backed actionable findings."
policy:
  allow_implicit_invocation: true
