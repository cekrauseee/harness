interface:
  display_name: "Harness Recall"
  short_description: "Pull only relevant project context"
  default_prompt: "Use $harness-recall to search semantic cards and hydrate only the project context this task needs."
policy:
  allow_implicit_invocation: true
